{
  "compilerOptions": {
    "outDir": "dist", // Optional: Hardhat usually handles its own artifacts
    "rootDir": ".",
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"]
    },
  },
  "include": ["functions/**/*.ts", "./test/**/*.ts", "./typechain-types/**/*.ts", "hardhat.config.ts","./functions/**/*.ts"],
  "exclude": ["node_modules", "dist", "artifacts", "cache"]
}